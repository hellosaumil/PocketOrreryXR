---
trigger: always_on
---

- If the shell commands don't work, make sure to `cd` to project root and retry before giving up.
- Never automatically make a commit, unless user explictly asks to save changes.
- always rerun app on the device